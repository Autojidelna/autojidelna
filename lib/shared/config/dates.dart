class Dates {
  static DateTime minimalDate = DateTime(DateTime.now().year, DateTime.now().month - 1, 1);
  static DateTime maximalDate = DateTime(DateTime.now().year, DateTime.now().month + 2, 0);
}
